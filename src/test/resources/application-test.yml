spring:
  application:
    name: OdengFinance-Test
  datasource:
    url: jdbc:postgresql://localhost:5432/playground_db
    username: postgres
    password: postgres
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        jdbc:
          time_zone: UTC

